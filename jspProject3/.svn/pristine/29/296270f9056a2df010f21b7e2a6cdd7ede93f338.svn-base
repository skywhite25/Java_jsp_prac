<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script type="text/javascript">
function jquery(){
	alert("test");
	nameFunc();
}
// 변수나 함수의 이름을 만드는 규칙 맨앞자가 영문자나 "_", "$"
function $(){
	jquery();
}
// $("selector") 객체를 선택할 때 -> td:tag, .td : class, #td:id
// 함수 전달해서 사용하는 방법
// 1. 변수에 함수를 저장했다가 실행하는 방법
var nameFunc = function(){
	alert("lee");
}
// 파라메터로 반드시 함수를 넘겨야 한다. 넘겨진 함수를 실행
function exe(func){
	func();
}

// 함수를 전달해서 실행하는 함수.
function paramFunc(){
	exe(function(){
		alert("전달된 함수");
	});
}
// 1. 이름을 입력 받아서 경고창을 띄고(본 처리문) -> 2. "처리가 완료 됨"을 출력하는 함수를 만들어서 처리 -> call back 함수
function callBack(name, callBackFunc){
	// 원래의 처리
	alert(name);
	// 처리가 다 끝나면 뒤에서 실행되는 함수
	callBackFunc();
}

// 콜백함수를 이용한 호출 함수
function call(name){
	callBack(name,function(){
		alert("처리가 완료 됨");
	});
}
</script>
</head>
<body>
<button onclick="jquery();">jquery()호출</button>
<button onclick="$();">$()호출</button>
<button onclick="paramFunc();">전달함수 실행</button>
<hr>
<button onclick="call('홍길동')">홍길동</button>
<button onclick="call('이영환')">이영환</button>
</body>
</html>